{
  "jira": {
    "type": "cloud",
    "baseUrl": "${JIRA_URL}",
    "email": "${JIRA_EMAIL}",
    "apiToken": "${JIRA_TOKEN}",
    "project": "SCRUM",
    "issueType": "Bug",
    "strictGDPR": true
  },
  "ai": {
    "mode": "assist",
    "provider": "openai",
    "apiKey": "${AI_API_KEY}",
    "model": "gpt-4",
    "temperature": 0.1,
    "maxTokens": 4000,
    "confidence": 0.7
  },
  "dedup": {
    "enabled": true,
    "windowHours": 24
  },
  "defaultLabels": ["ci-cd", "pipeline-failure", "automated"],
  "defaultAssignee": "${JIRA_ASSIGNEE}",
  "severity": {
    "critical": ["security", "production-down"],
    "high": ["database", "api-failure"],
    "medium": ["test-failure", "build-error"],
    "low": ["linting", "warning"]
  },
  "customFields": {
    "repository": "customfield_10010",
    "branch": "customfield_10011",
    "commit": "customfield_10012",
    "pipeline": "customfield_10013",
    "environment": "customfield_10014"
  },
  "notifications": {
    "slack": {
      "enabled": false,
      "webhook": "${SLACK_WEBHOOK}"
    },
    "teams": {
      "enabled": false,
      "webhook": "${TEAMS_WEBHOOK}"
    }
  }
}
